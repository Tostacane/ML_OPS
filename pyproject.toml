[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build"

[project]
name = "ml-ops"
version = "0.1.0"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra"
